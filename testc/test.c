#include <stdio.h>
#include "lib.h"

unsigned x,y;
int zz, zzz[20],z[35];

int f() {
  f();
  return 1;
}
int main() {
  int a = 0,b;
  x=a;
  printf("%d", a);
  f(a + 2);
  float fuc;
  sprintf("%lf scr", fuc );
  while (1) {
    lib_fun();
  }
}
