#include <stdio.h>
#include "lib.h"

unsigned x,y;
int zz, zzz[20],z[35];
/* int kk, kkk */
// ¹þ¹þ

int f() {
  f();
  return 1;
}
int main() {
  int a = 0,b;
  x=a;
  printf("int aa; %d ", a);
  f(a + 2);
  float fuc;
  int *s;
  if (a==0) {

  } else if(a==1) {

  } else{

  }
  sprintf(s, "%lf scr", fuc );
  while (1) {
    lib_fun();
  }
}
